# Copie para .env e preencha. NUNCA versione o .env com valores reais.

# --- Firebase (Admin SDK) ---
# Service account JSON inteiro, codificado em base64.
FIREBASE_SERVICE_ACCOUNT_BASE64=

# --- Autenticação (Firebase Auth — spec 016) ---
# Não há mais segredo de JWT: quem assina e verifica os tokens é o Firebase.
# Sem esta chave o login responde 500 apontando a variável — o Admin SDK cria e
# verifica contas, mas não valida senha nem envia e-mail.
#
# NÃO use a chave que o console do Firebase mostra em Configurações do projeto →
# Geral → "Chaves de API da Web". Ela costuma vir com restrição de HTTP referrer
# para os domínios do site, e uma chamada de servidor não manda `Referer`: o
# Google recusa com 403 "Requests from referer <empty> are blocked" e o login
# inteiro cai (spec 017).
#
# Crie uma chave separada, de servidor, em Google Cloud Console → APIs e
# serviços → Credenciais → Criar credenciais → Chave de API:
#   Restrições de aplicativo: Nenhuma (ou "Endereços IP", se o host tiver IP fixo)
#   Restrições de API: Identity Toolkit API + Token Service API, e só elas
#
# A chave precisa ser do **mesmo projeto** do FIREBASE_SERVICE_ACCOUNT_BASE64
# acima. Projetos diferentes fazem o login passar e o token ser recusado logo
# depois pelo Admin SDK, por `aud` divergente.
#
# Conferir antes de gravar (esperado: 400 INVALID_LOGIN_CREDENTIALS; 403 é chave
# ainda restrita):
#   curl -s -X POST "https://identitytoolkit.googleapis.com/v1/accounts:signInWithPassword?key=SUA_CHAVE" \
#     -H "Content-Type: application/json" \
#     -d '{"email":"naoexiste@example.com","password":"xxxxxxxx","returnSecureToken":true}'
FIREBASE_WEB_API_KEY=

# --- Servidor ---
PORT=8080
# Origens liberadas no CORS, separadas por vírgula.
CORS_ORIGINS=http://localhost:4200,https://barbarafarias.com.br

# --- Geração de material (Google Gemini) ---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest

# --- E-mail transacional (Resend) ---
# Única variável obrigatória: a chave da API.
RESEND_API_KEY=
# OPCIONAL. Sem esta variável o SDK usa o remetente compartilhado da Resend
# (onboarding@resend.dev), que já funciona. Defina o domínio próprio quando ele
# estiver verificado na Resend (SPF/DKIM no DNS) para enviar a qualquer destino.
# RESEND_FROM="Bárbara Farias <no-reply@barbarafarias.com.br>"

FIREBASE_STORAGE_BUCKET=

# --- Pagamentos (Mercado Pago — spec 023) ---
# Um provedor para os dois métodos: PIX à vista e cartão (parcelado no
# Semestral/Anual, assinatura recorrente no Mensal). Sem as chaves o módulo
# sobe normalmente e a contratação devolve o aviso de cobrança indisponível —
# nunca um 500.
#
# **São DUAS variáveis de token, e os prefixos divergem de propósito.** A Orders
# API usa token de teste com prefixo `APP_USR`; Assinaturas (`/preapproval`) e os
# Bricks usam `TEST-`. Os dois são válidos e nenhum está depreciado. Como esta
# integração usa Orders **e** Assinaturas, o ambiente de teste tem os dois
# ativos ao mesmo tempo. Quem vir um `APP_USR` num lugar que espera `TEST-` vai
# "corrigir" e quebrar o outro caminho — por isso os nomes são distintos.
#
# Em produção os dois recebem o mesmo access token da aplicação; a separação
# existe para o ambiente de teste.
#
#   Painel → Suas integrações → <aplicação> → Credenciais
MP_ACCESS_TOKEN_ORDERS=
MP_ACCESS_TOKEN_SUBSCRIPTIONS=
# Chave **pública** (`APP_USR-…` ou `TEST-…`). O backend não a usa: ela vive no
# `environment.ts` do front, que é quem monta o Payment Brick. Fica registrada
# aqui só para os dois jogos de credencial serem copiados juntos do painel.
MP_PUBLIC_KEY=
# Segredo da assinatura dos webhooks. Nasce ao salvar *Webhooks > Configurar
# notificações* no painel — não na criação da aplicação — e não expira.
#
# **Ausente = webhook recusado**, nunca "aceito sem conferir": sem a conferência
# da assinatura, um POST anônimo vira assinatura ativa.
#
#   produção  https://api.barbarafarias.com.br/mercadopago/webhook
#   staging   https://dev-api.barbarafarias.com.br/mercadopago/webhook
#
# Tópicos assinados: `order` (PIX e cartão), `subscription_authorized_payment`
# (cada ciclo do mensal) e `subscription_preapproval` (o Mercado Pago cancela a
# assinatura sozinho depois de 3 parcelas recusadas — sem este tópico o
# cancelamento dele nunca chega até nós).
#
# O painel tem um botão **Simular** que dispara uma notificação real contra a
# URL: é o caminho de teste, e ele existe antes de haver qualquer cobrança. O
# Mercado Pago não alcança `localhost` — exponha a porta com um túnel
# (`ngrok http 3000`) e aponte a URL de teste para ele.
MP_WEBHOOK_SECRET=

# --- Ambiente local ---
# Libera a rota POST /subscriptions/dev/mock-pay, que simula a confirmação do
# PIX no ambiente local. NUNCA `true` em produção.
DEV_MODE=false
# Base do frontend, usada nas URLs de retorno do checkout de cartão.
APP_BASE_URL=http://localhost:4200
